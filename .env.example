# Toolkit Intelligence Engine Configuration

# Gemini / Vertex AI Configuration (Optional for offline replay `npm run gate:b` and tests `npm test`)
# Required only if executing live LLM extraction runs via Google Cloud Vertex AI or Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Search provider configurations
# DUCKDUCKGO_API_KEY=
